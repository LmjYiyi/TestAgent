STEP_1_PROMPT = """
### 当前任务：准备测试数据
- **核心目标**: 安全地查询数据库，获取测试所需的前置数据。
### **严格执行步骤 (必须遵守)**:
1.  **执行数据库描述**:
    - **你的唯一指令**: 你 **必须** 调用 `run_sql_query` 工具，查询当前步骤描述中提及的**相关表**的结构。
    - **工具输入示例**: `{"query": "DESCRIBE <表名>;"}` (请根据当前步骤描述中的表名替换 `<表名>`)
    - **要求**：如果没有找到这个表或者其他失败的情况，请返回一个错误消息，不要再继续执行了。
2.  **执行数据查询**:
    - **你的唯一指令**: 在上一步成功获取表结构后，你 **必须** 调用 `run_sql_query` 工具，根据 `当前步骤描述` 和 `场景定义` 中的条件，查询相关表中的数据。
    - **工具输入示例**: `{"query": "SELECT <所需字段> FROM <表名> WHERE <条件> LIMIT 1;"}` (请根据当前步骤描述和场景定义中的信息构建查询)
    - **要求**：如果这一步失败，请返回一个错误消息，不要再继续执行了。
3.  **保存测试数据**:
    - **你的唯一指令**: 在上一步成功查询到数据后，你 **必须** 调用 `update_state` 工具，将查询到的数据保存到状态中。
    - **工具输入示例**: `{"state_object": {"test_data": [<你从数据库查询到的数据>]}}`
    - **要求**：如果这一步失败，请返回一个错误消息和目前执行得到的结果，不要再继续执行了。
- **禁止**: 在没有先执行 `DESCRIBE` 的情况下，直接执行 `SELECT` 查询。
- **禁止**: 禁止随意发挥、自由发挥，必须按照上述步骤进行，若无法执行，请如实将错误结果返回。
- **重要提示**: 确保严格按照顺序执行上述工具调用。
"""

STEP_2_PROMPT = """
### 当前任务：构造请求报文
- **核心目标**: 精确地根据指令，结合动态数据和固定值，生成最终的API请求报文。
- **你的唯一指令**: 你 **必须** 按顺序调用 `process_payload` 和 `update_state` 工具。

### 执行步骤 (必须严格遵守):
1.  **分析指令优先级**:
    - **最高优先级**: `当前步骤描述` 中明确指定的**固定值** (例如, `transfer_amount` 为 `1.00`)。这些值**必须**被直接使用，**不能**被 `test_data` 中的任何值覆盖。
    - **第二优先级**: `当前步骤描述` 中要求从 `test_data` 获取的**动态值** (例如, `card_no`, `person_name`)。
2.  **构建原始 `api_request_payload` 对象**:
    - 复制上下文中的 `api_request_payload` 模板。
    - **严格填充**:
        - 对于 `当前步骤描述` 中有固定值的字段，直接使用该固定值。
        - 对于 `当前步骤描述` 中要求使用动态数据的字段，从 `test_data[0]` 中获取并填充。
        - 其他未提及的字段，保持模板中的默认值不变。
3.  **调用 `process_payload` 工具**:
    - **你的唯一任务**: 调用 `process_payload` 工具处理原始报文。
    - **输入**: 将步骤2中构建的原始报文作为 `process_payload` 的参数。
    - **功能**: 该工具会自动替换日期时间占位符并清理空值字段。
4.  **调用 `update_state` 工具**:
    - **你的唯一任务**: 调用 `update_state` 工具保存处理后的报文。
    - **输入**: 将 `process_payload` 返回的处理后报文作为 `update_state` 的参数，格式为：`{{"api_request_payload": <process_payload返回的处理后报文>}}`。

### 绝对禁止:
- **禁止覆盖**: 绝对禁止使用 `test_data` 中的值去覆盖 `当前步骤描述` 中已明确指定的固定值。例如，即使 `test_data` 中有 `usable_amount`，也不能用它来填充 `transfer_amount` 字段，因为步骤描述已经规定了 `transfer_amount` 的值。
- **禁止臆测**: 只能根据上述规则进行填充，禁止任何形式的自由发挥或智能猜测。
- **禁止跳过**: 必须按顺序执行 `process_payload` 和 `update_state` 两个工具调用，不能跳过任何一个。
"""

STEP_3_PROMPT = """
### 当前任务：执行API调用并保存结果
这是一个连续的、自动化的过程。你必须严格按顺序执行以下工具调用。

### **严格执行步骤 (必须遵守)**:
1.  **检查前提条件**:
    - **验证**: 检查上下文中 `已构造的请求体 (API Request Payload)` 是否存在且不为 `null`。
    - **如果缺失**: 你 **必须** 停止执行并输出错误消息："无法执行API调用，因为上一步未能成功构造请求报文"。
    - **如果存在**: 继续下一步。

2.  **调用API**:
    - **你的唯一任务**: 调用 `call_api` 工具。
    - **输入**: **必须**使用上下文中已保存的、完整的 `api_request_payload` 作为 `call_api` 的参数。

3.  **保存API响应**:
    - **触发条件**: `call_api` 工具成功返回响应后。
    - **你的唯一任务**: 立即调用 `update_state` 工具。
    - **输入**: 将 `call_api` 返回的**完整的、未经修改的**JSON响应，包装后作为 `update_state` 的参数。格式必须是: `{{"last_api_response": <完整的API响应>}}`。

**绝对禁止**:
- 在 `api_request_payload` 为 `null` 时尝试调用 `call_api`。
- 在一次思考中遗漏上述任何一个步骤。你必须连续完成`call_api`和`update_state`的调用。
"""

STEP_4_PROMPT = """
### 当前任务：校验与断言
- **核心目标**: 对比API的实际响应和预期结果，并根据步骤描述完成所有校验点。
- **你的唯一指令**: 你 **必须** 调用 `update_state` 工具一次，以 `{{"assertion_result": {{"result": "成功/失败", "details": "..."}}}}` 的格式报告你的最终断言结论。

### 断言规则 (必须严格遵守):
1.  **基础校验 (必须执行)**:
    - **HTTP状态码**: 验证API响应的HTTP状态码为 200。
    - **响应体核心字段**: 根据 `当前步骤描述` 中的具体要求进行校验。如果步骤描述中明确指定了预期的 `return_code` 和 `return_msg`，则按描述进行校验；如果没有明确指定，则根据业务逻辑判断。

2.  **动态校验 (按需执行)**:
    - **仔细阅读 `当前步骤描述`**: 查找其中提到的**所有**具体断言要求。
    - **示例1 (数据库校验)**: 如果步骤描述要求“查询`credit_card_account`表，验证`usable_amount`未发生变化”，你 **必须** 调用 `run_sql_query` 工具执行数据库查询，并对比结果。
    - **示例2 (响应内容校验)**: 如果步骤描述要求“确保响应体 `data` 中的 `field1` 字段值符合预期”，你 **必须** 检查 `last_api_response.data.field1` 的值。
    - **示例3 (余额变化计算)**: 如果步骤描述要求“对比交易前后`usable_amount`的差值是否等于`transfer_amount`”，你 **必须** 结合 `test_data` 和 `run_sql_query` 的结果进行计算和比较。

3.  **业务规则验证 (最高优先级)**:
    - **业务规则是最高优先级**: 如果步骤描述中包含任何业务规则验证要求（如手续费计算、余额变化规则等），这些验证**必须**通过，否则整体断言判定为失败。
    - **示例**: 如果步骤描述中包含任何业务规则验证要求（如计算结果、余额变化、状态变更等），但实际结果与预期不符，则**必须**判定为失败，即使其他技术指标（如HTTP状态码、数据库一致性等）正常。
    - **禁止**: 禁止因为"API响应正常"或"数据库一致性"而忽略业务规则验证失败的情况。

4.  **综合结论**:
    - **成功条件**: 所有基础校验、动态校验和业务规则验证**全部通过**。
    - **失败条件**: 任何一项校验失败，包括但不限于：
        - HTTP状态码不是200
        - 响应体核心字段不符合预期
        - 业务规则验证失败（如手续费计算错误、余额变化不符合预期等）
        - 数据库查询结果与预期不符
    - **示例调用**: 
        - 成功: `{{"assertion_result": {{"result": "成功", "details": "所有断言均通过，交易成功且扣款金额符合预期。"}}}}`
        - 失败: `{{"assertion_result": {{"result": "失败", "details": "业务规则验证失败：实际结果与预期不符，具体错误信息..."}}}}`
"""
